import axios from "axios";

const state = {
    products: {}
};

const getter = {};
const actions = {};
const mutations = {};

export default{
    namespaced: true,
    state,
    getter,
    actions,
    mutations
}